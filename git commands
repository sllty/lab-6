git init는 새로운 Git저장소를 초기화한다. 

git status는 현재 작업 디렉토리의 상태를 확인한다. 

git add[file_name]은 파일을 스테이징 영역에 추가하여 변경 사항을 기록한다. 

여기서 [file_name]은 스테이징 영역에 추가하려는 파일의 이름이며, 파일 이름을 사용하여 해당 파일을 스테이징한다. 

스테이징한 파일은 이후에 git commit 명령어를 사용하여 커밋할 수 있다. 

git rm --cached[file_name]은 Git 저장소에서 특정 파일을 제거하고, 그 파일을 인덱에서만 제거하여 로컬 작업 디렉토리에는 남겨둔다.  

[file_name]은 제거하려는 파일의 이름이며, 이를 통해 해당 파일을 Git의 추적에서 제외할 수 있다. 

Git에서 파일을 무시하려면 .gitignore 파일을 사용한다. 

이 파일에는 Git이 무시해야 하는 파일 또는 디렉토리의 패턴 목록이 들어 있다. 

git commit -m "commit message"은 Git에서 변경 사항을 로컬 저장소에 커밋하는 명령어이다. 

이 명령어를 사용하면 지정한 메시지를 가진 새로운 커밋을 생성한다.

"commit message"는 해당 커밋에 대한 설명이나 메시지를 나타낸다.

커밋 메시지는 변경 사항의 요약이나 설명으로, 이 커밋이 어떤 변경 사항을 포함하고 있는지 알려주는 역할을 한다.

git branch는 브랜치를 생성, 삭제, 나열한다.
